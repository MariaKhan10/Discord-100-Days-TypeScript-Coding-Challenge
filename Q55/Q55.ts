// Double numbers in an array: Make a list of numbers.Then,use a trick to make a list where each number is twicw its original value.

let numbers = [1,2,3,4,5,6]
let doubledNumbers = numbers.map(number => number * 2) ;

console.log(doubledNumbers)
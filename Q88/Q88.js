// Rounding to the nearest integar:Write a function that takes a decimal number as input and returns the number rounded to the nearest integar.
function roundNumber(num) {
    return Math.round(num);
}
console.log(roundNumber(6.2));
console.log(roundNumber(8.7));
